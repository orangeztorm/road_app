enum StatusEnum {
  unknown,
  busy,
  error,
  success,
  notFound,
}

enum LoginStatus { unknown, busy, error, success, resetPinRequired }

enum AuthenticatedStatus { authenticated, unauthenticated }
